- name: Create Minecraft User
  become: true
  ansible.builtin.user:
    name: minecraft
    password_lock: true
    state: present
    shell: /usr/bin/nologin
    comment: User owning minecraft server
    groups:
      - raid
