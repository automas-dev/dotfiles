#!/bin/bash

cd_to_root() {
	while [[ $PWD != "/" && ! -f .mk ]]; do
		cd ..
	done

	if [[ ! -f .mk ]]; then
		echo You are not in a project directory!
		exit 1
	fi
}

load_project() {
	cd_to_root
	source .mk
}

